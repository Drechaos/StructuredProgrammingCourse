
#include <iostream>

using namespace std;

int main(){
    // if(condition){
        // if the condition evaluates to true
   // }else{
        // if the condition evaluates to false
    //}
    int day = 1; // assumption is that the first day of the week is assigned the integer 1
    // comparative operator - equivalent
    if(day == 1){
        cout << "Go to church" << endl;
    }else{
        cout << "Go to work" << endl;
    }

    //system("pause"); - included for windows users
    return 0;
}