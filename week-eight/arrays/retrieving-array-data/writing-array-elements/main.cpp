#include <iostream>
using namespace std;

int main(){
    // Declaring array sample of size 3.
    int sample[3] = {10, 20, 30};
    int numOne;
    // Using cout, we can print the element at index 2
    // of the array
    cout << "The element at index 2 is " << sample[2];
    system("pause");
    return 0;
}