#include <iostream>
using namespace std;

int main(){
    int max = 3;
    // Declaring array sample of size 3.
    int sample[3] = {10, 20, 30};
    for(int i = 0; i < max; i++){
        cout << sample[i];
    }
    system("pause");
    return 0;
}