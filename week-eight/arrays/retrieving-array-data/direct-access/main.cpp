#include <iostream>
using namespace std;

int main(){
    // Declaring array sample of size 3.
    int sample[3] = {10, 20, 30};
    int numOne;
    // Using the assignment operator, the element at index 2 is 
    // assigned to the variable numOne.
    numOne = sample[2];
    system("pause");
    return 0;
}