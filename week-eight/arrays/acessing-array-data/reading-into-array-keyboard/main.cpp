#include <iostream>

using namespace std;

int main(){
    //Declaration of integer array
    int integers[3];
    // Prompt used to instruct user
    cout << "Enter an integer value: ";
    // Accepting input from keyboard using cin and storing
    // data to index 2 of the array
    cin >> integers[2];
    system("pause");
    return 0;
}