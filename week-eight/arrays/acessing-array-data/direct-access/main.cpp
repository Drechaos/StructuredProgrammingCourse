#include <iostream>

using namespace std;

int main(){
    //Declaration of integer array
    int integers[3];
    // The value -22 is being assigned to index 2 of the array
    // using the assignment operator (=)
    integers[2] = -22;
    return 0;
}